"""Network Attack Visualiser — backend application package.

Phase 0.5 (Tooling and CI Baseline): this package intentionally contains no
application logic yet. It exists so the linting, type-checking and test tooling
has real, typed Python to validate. Application modules (``config``, ``models``,
``detection``, ``alerts``, ``storage``, ``api``, ``ingest``, ``ai``) are
introduced in later approved phases — see ``docs/DEVELOPMENT_PHASES.md``.
"""

__version__: str = "0.0.0"

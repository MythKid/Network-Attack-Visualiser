# Development-tool dependencies for the Network Attack Visualiser.
#
# Pip-compatible and intentionally minimal — no Poetry/uv/Pipenv/Conda.
# These are DEVELOPMENT tools only; application (runtime) dependencies live in
# requirements.txt.
#
# Exact pins on these direct dependencies reduce unintended dependency drift;
# their transitive dependencies are resolved by pip and are not locked here.
# Bump deliberately (e.g. via a reviewed PR), not implicitly.

ruff==0.15.21
mypy==2.3.0
pytest==9.1.1
pre-commit==4.6.0

# HTTP client used by FastAPI/Starlette's TestClient for API tests. The current
# Starlette TestClient prefers httpx2 and deprecates its older httpx fallback.
# Test-only — never a runtime dependency (see requirements.txt).
httpx2==2.7.0
